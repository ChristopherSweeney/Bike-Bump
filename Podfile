# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Bike Bump' do
  # Comment the next line if you're not using Swift and don't want to use 
  # dynamic frameworks
  use_frameworks!
  pod 'Firebase/Core'
  pod 'Firebase/Auth'
  pod 'FirebaseUI', '~> 1.0' 

  target 'Bike BumpTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Bike BumpUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
